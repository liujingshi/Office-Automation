<!--pages/message/message.wxml-->
<view>
	<view wx:for="{{message}}" class="panel-item">
		<van-card bindtap="chat" data-userid="{{item.userid}}" desc="{{item.dt}}" tag="{{item.num}}" title="{{item.name}}" thumb="{{item.head}}" />
	</view>
</view>
